{
  "name": "aliaspaces-social-platform",
  "version": "0.1.0",
  "private": true,
  "description": "First-party social product for owner-controlled personas.",
  "type": "module",
  "scripts": {
    "test": "node --test apps/social-web/tests/*.test.mjs && node scripts/check-boundaries.mjs",
    "test:boundaries": "node scripts/check-boundaries.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
