# AliaSpaces Edge Functions

No Edge Function is copied at this checkpoint. The existing social functions
are coupled to the transitional application or shared platform authority.

Only independently reviewed AliaSpaces social functions may be added here. CI
must reject any function also present in the MyPersonas deployment allowlist.
Provider tokens, service-role behavior, automation workers, billing handlers,
and private research functions are prohibited.
